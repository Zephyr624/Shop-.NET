@page
@model WebApplication3.Pages.CategoryDetailsModel
@{
}
@{
                string id = Request.Query["id"].ToString();
                Model.ID = Convert.ToInt32(id);
                int point = Model.categoryList.FindIndex(p=>p.id==Model.ID);
}
<h3>Szczegóły</h3>
<h4>Oto szczegóły Kategorii: @Html.DisplayFor(model =>Model.categoryList[point].longName)</h4>
 <div class="container">
 <form class="form-horizontal" method="post">
 <div class="form-group">
            
    <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Category.shortName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model =>Model.categoryList[point].shortName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Category.longName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model =>Model.categoryList[point].longName)
        </dd>
            
    </div>    
 <div class="form-group">
 <div class="col-sm-offset-2 col-sm-10">
 <a asp-page="CategoryList">Wróć do listy</a>
 <button class="btn btn btn-primary">dodaj do koszyka</button>
 </div>
 </div>
 </form>
 </div>
